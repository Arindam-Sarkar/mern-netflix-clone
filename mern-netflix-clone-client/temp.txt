useEffect(() => {
    console.log('effect');

    const fetchDataFromId = () => {
      setFavMovieData([{}])
      // Get movie data from movie id
      let favMovieDataTmp = new Array({})
      favouriteMovieIds.map(async (movieId) => {
        try {
          let queryStr = `https://api.themoviedb.org/3/movie/${movieId}?api_key=${MOVIEDB_API_KEY}&language=en-US`
          let resp = await axios.get(queryStr)
          const { backdrop_path, id, title, ...remaining } = await resp.data
          const objTmp = { backdrop_path, id, title }

          let present = [false]
          console.log("favMovieData.length", favMovieData.length)
          if (favMovieData.length !== 0) {
            let present = favMovieData.map(movie => {
              if (movie.id === objTmp.id) {
                console.log("present")
                return true
              }
              else {
                console.log("absent")
              }
            })
          }

          if (present.indexOf(false) !== -1) {
            setFavMovieData(prev => [...prev, objTmp])
          }


        } catch (error) {
          // console.log(error);
        }
      })

    }

    fetchDataFromId()

  }, [0])